# estado local do usuario (cada maquina tem o seu)
state.json

# Python
__pycache__/
*.pyc

# Node
node_modules/
dist/
dist-types/
build/
coverage/
*.tsbuildinfo

# macOS
.DS_Store

# logs e temporarios
*.log
*.tmp

# store local do pnpm (aparece quando o store global nao e gravavel)
.pnpm-store/

# capturas da verificacao visual (geradas, nao versionadas)
capturas/

# saida do empacotamento do app
release/

# saida do empacotador (gerada a cada build)
apps/desktop/build/
